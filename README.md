# ImCloneDetect

## Instructions

1. Open a terminal in the `ImCloneDetect/main` directory.
2. Execute the command provided in `commands.txt`.
3. Ensure input images are present in the `inputImages/` directory.
4. Output results will be generated in the `outCSV/` directory as CSV files.